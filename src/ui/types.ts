// agent_plugin_dev/ui-chat-plugin/src/ui/types.ts —— 前端类型聚合(复用 registry.ts 的消息/角色类型)
export type { ChatRole, ChatMessage, ChatPageCtx, BubbleApi, ToolApi, ToolScope, RegionSlot, PageItem, BubbleItem, ToolItem, RegionItem, RegKind } from '../registry.ts'
