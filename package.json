{
  "name": "chat-plugin",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "纯后端插件:/api/chat/* 路由 + history/input prompt 注入,消费 multiSession/prompt/llm;对话页渲染已移交 ui-chat-plugin(cordis v4)",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "cordis": "file:../../agent_plugin_feat/vendor/cordis"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3",
    "vitest": "^4.1.11"
  },
  "st": {
    "bundle": { "patch": "./cordis.patch.yml" }
  }
}
